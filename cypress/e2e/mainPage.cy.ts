// Only bare minimum tests are written here as most cases are handled by unit tests
describe("Index Page", () => {
  it("should display a loader while fetching", () => {});
  it("should display error messages when fetching itineraries fails", () => {});
  it("should let the user search itineraries using all the fields", () => {});
  it("should display the results when the user searches", () => {});
  it("le you go back to search page", () => {});
});
